import { loadSampleData, clearSampleData } from '../mockData/sampleClients';

// Utility to easily load sample data from browser console or component
export { loadSampleData, clearSampleData };

// You can run this in browser console to load sample data:
// import { loadSampleData } from './src/utils/loadSampleData.ts'
// loadSampleData()

console.log('Sample data utility loaded. Use loadSampleData() to populate test data.');
